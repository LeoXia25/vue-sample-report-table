/**
 * vue-sample-report-table 组件库入口
 *
 * 支持两种导入方式：
 * 1. 全量导入：import SampleReportTable from 'vue-sample-report-table'
 * 2. 按需导入：import { default as SampleReportTable } from 'vue-sample-report-table'
 */

// 全量导出
import SampleReportTable from '../components/SampleReportTable/index.vue'

export default SampleReportTable

// 按需导出（备选）
export { SampleReportTable }
